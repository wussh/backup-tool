# ============================================================================
# BACKUP METHOD CONFIGURATION
# ============================================================================
# Choose backup method: "docker-run", "docker-exec", or "kubectl-exec"
# - docker-run:    Use temporary container (no need for running container)
# - docker-exec:   Exec into existing Docker container
# - kubectl-exec:  Exec into Kubernetes pod
BACKUP_METHOD=docker-run

# For docker-exec: specify container names
PG_CONTAINER=test-postgres
MYSQL_CONTAINER=test-mysql
MARIADB_CONTAINER=test-mariadb
MONGO_CONTAINER=test-mongodb

# For kubectl-exec: specify pod names and namespace
PG_POD=postgres-0
MYSQL_POD=mysql-0
MARIADB_POD=mariadb-0
MONGO_POD=mongodb-0
K8S_NAMESPACE=default

# Backup destination directory (used for docker-exec and kubectl-exec)
# This is where backups will be copied from containers
BACKUP_TEMP_DIR=/tmp/db-backups

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL Configuration
PG_HOST=postgres
PG_USER=postgres
PG_DB=mydb
PG_PASS=password
PG_VERSION=15  # Docker image version: postgres:15

# MySQL Configuration
MYSQL_HOST=mysql
MYSQL_USER=root
MYSQL_PASS=password
MYSQL_DB=mydb
MYSQL_VERSION=8  # Docker image version: mysql:8

# MariaDB Configuration (can use same vars as MySQL or separate)
MARIADB_HOST=mariadb
MARIADB_USER=root
MARIADB_PASS=password
MARIADB_DB=mydb
MARIADB_VERSION=11  # Docker image version: mariadb:11

# MongoDB Configuration
MONGO_HOST=mongodb
MONGO_DB=mydb
MONGO_VERSION=7  # Docker image version: mongo:7
